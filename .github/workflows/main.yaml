name: CI/CD Pipeline

on:
  workflow_dispatch:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions: {}

jobs:

  example:
    name: Example Job
    runs-on: ubuntu-latest
    steps:
      - name: Hello world
        run: echo "Hello from CI/CD Pipeline (main.yaml)" && exit 1
